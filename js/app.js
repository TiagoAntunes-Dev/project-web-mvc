/**
 * app.js — Ponto de entrada da aplicação
 * Inicializa o Controller após o DOM estar pronto
 * Sem lógica de apresentação ou dados aqui
 */
document.addEventListener("DOMContentLoaded", () => {
  AppController.init();
});
